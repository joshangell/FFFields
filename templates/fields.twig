{% if element is not defined %}{% set element = null %}{% endif %}
{% if namespace is not defined %}{% set namespace = 'fields' %}{% endif %}

<div class="ui form">
{% for field in fields if field.getField().handle not in excludedFieldHandles %}
    {{ craft.fffields.renderField({
        element: element,
        fieldLayoutField: field,
        namespace: namespace,
        assetsFolderId: assetsFolderId
    }) }}
{% endfor %}
</div>